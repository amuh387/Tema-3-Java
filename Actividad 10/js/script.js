function Calificar() {
    let alumnos = ["Pepe", "Jorge", "Juan", "Andrés"];
    let notas = [];
    for (let i = 0; i < 4; i++) {
        let nota = (prompt("Ingrese la nota para " + alumnos[i] + ":"));
    }
}
